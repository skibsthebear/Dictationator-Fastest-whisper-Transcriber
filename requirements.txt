# If you want GPU support, make sure you have CUDA installed with a supported GPU.
# Note: Install PyTorch separately with the correct CUDA version for your system:
# Check your CUDA version with: nvidia-smi

pyaudio
keyboard
faster-whisper
watchdog
pyperclip
PySide6
python-dotenv
transformers

